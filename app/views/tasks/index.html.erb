<h1>Tasks</h1>

<%= link_to "New", new_task_path %>

<% unless @tasks.empty? %>
<ul>
    <% @tasks.each do |task| %>
        <li>
            <%= link_to task.title, task %>
        </li>
    <% end %>
</ul>
<% end %>