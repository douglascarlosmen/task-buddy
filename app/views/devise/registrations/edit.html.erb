<% content_for :h2title do %>
  Minha conta
<% end %>

<%= render 'form_edit' %>

<%= button_to "Clique aqui para EXCLUIR a sua conta", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Tem certeza que deseja excluir a sua conta para sempre?" }, method: :delete, class: "btn btn-lg btn-block btn-danger btn-width-full mt-3" %>

<%= render 'partials/back_button' %>
